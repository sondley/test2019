export declare class EsendexFake {
    private server;
    private app;
    private dispatcherRequests;
    constructor();
    listen(port: any, cb: any): void;
    close(): void;
    private MessageDispatcherEndpoint(req, res);
    private MessageHeaderEndpoint(req, res);
    private MessageHeadersEndpoint(req, res);
    private AccountEndpoint(req, res);
    private AccountsEndpoint(req, res);
}
