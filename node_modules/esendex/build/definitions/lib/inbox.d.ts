export interface InboxGetOptions {
    accountreference?: string;
}
export interface InboxUpdateOptions {
    id: string;
    read?: boolean;
}
export declare class Inbox {
    private client;
    private parser;
    constructor(client: any);
    get(options: InboxGetOptions, callback: (err: any, messageheaders?: any) => void): void;
    update(options: InboxUpdateOptions, callback: (err: any) => void): void;
    delete(id: any, callback: any): void;
}
