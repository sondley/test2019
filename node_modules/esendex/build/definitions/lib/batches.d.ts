import { Client } from './client';
export declare class Batches {
    private client;
    private builder;
    private parser;
    constructor(client: Client);
    get(options: any, callback: any): void;
    cancel(batchId: any, callback: any): void;
    rename(options: any, callback: any): void;
}
