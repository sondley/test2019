import { Client } from './client';
export interface MessagesGetOptions {
    id?: string;
}
export declare class Messages {
    private client;
    private builder;
    private parser;
    constructor(client: Client);
    get(options: MessagesGetOptions, callback: any): void;
    send(messages: any, callback: any): void;
    getBody(messageId: any, callback: any): void;
}
